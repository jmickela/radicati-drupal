{% set attributes = attributes.removeAttribute('role') %}

{# summary is set in theme_preprocess_node #}

{% include "@organisms/90-detail-listing-card/_detail-listing-card.twig" with {
  promoted: promoted,
  sticky: sticky,
  published: published,
  view_mode: view_mode,
  content_type: node.bundle|clean_class,
  title_prefix: title_prefix,
  title_suffix: title_suffix,
  page: page,
  url: url,
  label: label,
  content: content,
  summary: summary,
  attributes: attributes,
  created: node.createdtime|format_date('short_month_day_year'),
} only %}
